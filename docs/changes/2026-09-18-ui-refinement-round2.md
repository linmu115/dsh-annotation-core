# 引用浮窗精简（第二轮）

根据真实截图反馈修正。保留原展开方式与引用功能，删除详情标题栏、活跃编号和来源元数据行；关闭按钮保留右上角。统一文案为「注释」，不再显示快捷键说明。多来源切换保留，通过短标题或原文片段识别。

- 浮窗宽度 460 → 326 px，编辑区高度 112 → 80 px；正常内容的面积约原先一半，长内容内部滚动。
- 只读与编辑注释均无内边框、阴影或内层背景；删除重复旧样式，集中一份声明。
- 底部操作全部右对齐；30 × 30 px、50% 圆角并显式设置 corner-shape: round，避免宿主超椭圆样式影响。
- 数量入口仅显示数字，保留完整无障碍标签。数量入口的原生dock复用输入卡片的左右留白与最大宽度；浮窗左边缘对齐实际 composer card；待发送保留悬停展开，已发送无锚点时使用唯一可见输入框定位。
- Enter 保存、Shift+Enter 换行、IME 不误提交、外部取消与切换引用撤销行为保留。

验证：typecheck/build 通过。全量 262 项中 260 项先通过，2 项旧文案断言随数字入口更新后，相关 3 文件 26 项通过；最终定位调整后 5 项对话测试再次通过。浏览器真实组件+合成资料验证单条/多条、外部撤销、Enter保存及只读详情；按钮实测30×30、50%/round，浮窗326px且与输入框同left，输入框border0/background透明/shadow none。

未安装到生产实例；不代表真实历史定位、跨端写入及多插件混合运行已验收。来源内容仅使用合成文本。
