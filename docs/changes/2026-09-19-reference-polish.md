# 引用与贴纸交互修订

删除注释浮窗顶部占位，让摘要与关闭按钮同排。内置选文“添加到当前会话”成功后立即打开该引用的可选注释编辑卡片；留空不阻止引用，Enter 保存、Shift+Enter 换行及点击外部撤销沿用原行为。

验证：类型检查、构建以及选择菜单、注释对话框、跨会话固定来源与图引用、高光生命周期等 40 项定向测试通过。

仅更新客户端产物，保留已安装宿主逻辑、版本兼容声明与其他插件。实例界面由用户验收，不能用合成测试代替。
