# dsh-annotation-core 0.1.0 本地候选包记录

日期：2026-08-24  
候选包：`dsh-annotation-core-0.1.0.tgz`

## 本阶段完成内容

- 固定版本 `0.1.0` 和官方 DSH `0.1.1-rc.2` peer 版本。
- 补齐根入口、Client、协议、Client API、Host API、Typert 与 Remote 七组公共导出。
- 包元数据只声明一个 Host patch 和一个 web Client bundle。
- Client 启动依赖固定为官方 `dsh-client-runtime`、`dsh-client-ui-conversation` 和 `dsh-client-ui-input-trigger`。
- Zod 被打入 Client bundle，运行时只向官方模块表请求 React、Cordis 和 client-runtime，避免动态依赖漂移。
- 中文和英文 README 改为用户安装、使用和常见问题说明；明确 core 没有独立看板。
- `pnpm pack` 内容已核对，测试夹、视觉对照 HTML、源码和开发文档均未进入安装包。

## Disposable profile 验收

1. 在 Windows 临时 `DSH_HOME` 初始化全新官方 `web` profile。
2. 通过 `dsh plugin --profile web add <本地 tgz>` 安装候选包。
3. 官方 `--dump-config` 成功，且只出现一个：

   ```text
   id: annotation-core
   name: dsh-annotation-core
   ```

4. 在独立端口 `127.0.0.1:41987` 启动官方 `0.1.1-rc.2`。
5. HTTP 首页返回 200；启动图中包含一个 `dsh-annotation-core` Client entry 和三项预期 inject。
6. 使用本机 Chrome 无界面打开真实页面，页面标题与主界面正常，无 `pageerror` 或 console error。
7. `style[data-plugin="dsh-annotation-core"]` 已注入，证明 Client factory 真实执行。
8. 停止临时 DSH，并删除已验证的临时目录；正式 `3080` 环境没有被改动。

## 自动验收

- `pnpm typecheck`：通过。
- `pnpm test`：通过。
- `pnpm build`：通过。
- `tests/bundle-contract.test.ts`：公共导出、peer、单实例 patch、模块表隔离和普通 Node 协议导入通过。

## 发布边界

本阶段只生成本地 tgz。没有发布 npm、没有推送 GitHub、没有安装到正式 `web` profile。
