# Core 0.3.12-rc2.29

运行环境：DSH 0.1.5-rc.2 / web。Core 不依赖 Bridge、Maintenance、DAG 或贴纸。

卸载先停止接收任务，再等待回链、提交和持久化收尾，最后关闭两个存储域。初始化中卸载时晚到资源会直接释放。公共来源 adapter 合同保持兼容；已发出的不可取消调用需等待其完成。

完整回归 308 项、类型检查和构建通过；最终安装与真实 UI/热重载未验收。升级前正常停机并备份 home，保留原配置和数据。
