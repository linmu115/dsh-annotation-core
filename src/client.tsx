import type { Context } from './context-types.ts'

export const inject: readonly string[] = []

export function apply(_ctx: Context): void {
  // Client behavior begins after the Task 1 feasibility gate passes.
}
